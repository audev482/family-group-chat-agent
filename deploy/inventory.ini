# One butler, one box. `connection: local` is the point of this file: the play runs
# on the machine you are sitting at, with no SSH, no bastion and no key to rotate.
#
# To deploy to a different machine instead, replace the host line with its address
# and drop ansible_connection.
[butler]
localhost ansible_connection=local
